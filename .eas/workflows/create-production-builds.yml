name: Create Production Builds

on:
  push:
    branches:
      - main

jobs:
  build_android:
    name: Build Android app
    type: build
    params:
      platform: android
      profile: production
